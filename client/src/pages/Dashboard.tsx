import React, { } from 'react';
import TextCard from '../components/dashboard/TextCard';

const AdminDashboard: React.FC = () => {
  return (

    <TextCard heading='as' description='as' />
  );
};

export default AdminDashboard;
